{
	"scripts": {
		"dev": "docker-compose up -d backend",
		"build": "cd apps/backend && dotnet build && cd ../frontend && npm run build",
		"build:backend": "cd apps/backend && dotnet build",
		"build:frontend": "cd apps/frontend && npm run build",
		"start:backend": "cd apps/backend && dotnet run",
		"start:frontend": "cd apps/frontend && npm run start"
	}
}
